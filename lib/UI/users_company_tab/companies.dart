import 'package:flutter/material.dart';

class CompaniesUsers extends StatelessWidget {
  const CompaniesUsers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Companies'),
      ),
    );
  }
}
